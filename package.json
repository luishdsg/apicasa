{
  "name": "apicasa",
  "version": "1.0.0",
  "description": "ok",
  "main": "s",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "json-server --watch db.json --port 3000",
     "vercel-build": "npm install",
     "build": "mkdir dist && echo 'Fake dist directory created'"
  },
    "dependencies": {
      "json-server": "^1.0.0-beta.2"
  },  
  "author": "",
  "license": "ISC"
}
